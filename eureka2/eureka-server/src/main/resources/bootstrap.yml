spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8888
#  profiles:
#    active: peer1
management:
  endpoints:
    web:
      exposure:
        include: '*'