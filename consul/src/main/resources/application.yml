spring:
  application:
    name: consul-8081
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        health-check-path: /
        health-check-interval: 5s
server:
  port: 8081